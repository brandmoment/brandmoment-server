# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: signup-to-login-navigation.spec.ts >> Scenario: Signup to Login Navigation >> should navigate from signup back to login via Sign in link
- Location: tests/smoke/signup-to-login-navigation.spec.ts:19:7

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: locator.click: Test timeout of 30000ms exceeded.
Call log:
  - waiting for getByRole('link', { name: 'Sign in' })

```

# Page snapshot

```yaml
- generic [ref=e2]: Internal Server Error
```

# Test source

```ts
  1  | import { test, expect } from "@playwright/test";
  2  | import path from "path";
  3  | import fs from "fs";
  4  | 
  5  | const SCENARIO = "signup-to-login-navigation";
  6  | const RUN_ID = new Date().toISOString().replace(/[:.]/g, "-").slice(0, 19);
  7  | const RESULTS_DIR = path.join(
  8  |   __dirname,
  9  |   "results",
  10 |   SCENARIO,
  11 |   RUN_ID
  12 | );
  13 | 
  14 | test.beforeAll(() => {
  15 |   fs.mkdirSync(RESULTS_DIR, { recursive: true });
  16 | });
  17 | 
  18 | test.describe("Scenario: Signup to Login Navigation", () => {
  19 |   test("should navigate from signup back to login via Sign in link", async ({
  20 |     page,
  21 |   }) => {
  22 |     // Step 1: Navigate to /signup
  23 |     await page.goto("/signup");
  24 |     await page.screenshot({
  25 |       path: path.join(RESULTS_DIR, "step-01-signup-page.png"),
  26 |       fullPage: true,
  27 |     });
  28 | 
  29 |     // Step 2: Click "Sign in" link
> 30 |     await page.getByRole("link", { name: "Sign in" }).click();
     |                                                       ^ Error: locator.click: Test timeout of 30000ms exceeded.
  31 |     await page.screenshot({
  32 |       path: path.join(RESULTS_DIR, "step-02-click-signin.png"),
  33 |       fullPage: true,
  34 |     });
  35 | 
  36 |     // Step 3: Assert URL is /login
  37 |     await expect(page).toHaveURL(/\/login/);
  38 |     await page.screenshot({
  39 |       path: path.join(RESULTS_DIR, "step-03-url-login.png"),
  40 |       fullPage: true,
  41 |     });
  42 | 
  43 |     // Step 4: Assert login heading is visible
  44 |     await expect(
  45 |       page.getByRole("heading", { name: "Sign in" })
  46 |     ).toBeVisible();
  47 |     await page.screenshot({
  48 |       path: path.join(RESULTS_DIR, "step-04-login-heading.png"),
  49 |       fullPage: true,
  50 |     });
  51 |   });
  52 | });
  53 | 
```